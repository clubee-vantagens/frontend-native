import { StyleSheet, View } from "react-native";

export default HrComponent = (first) => {
  return (
    <View
      style={{
        borderBottomColor: "black",
        borderBottomWidth: StyleSheet.hairlineWidth,
      }}
    />
  );
};
